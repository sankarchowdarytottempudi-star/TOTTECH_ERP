/**
 * @format
 */

import { AppRegistry } from 'react-native';
import CorporateApp from './CorporateApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => CorporateApp);
