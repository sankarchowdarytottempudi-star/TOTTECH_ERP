"use client";

import ParentPortalView from "../_components/ParentPortalView";

export default function ParentExamsPage() {
  return (
    <ParentPortalView
      view="exams"
      title="Exams"
      subtitle="Read-only exam schedule and assessment dates for your linked student."
    />
  );
}
