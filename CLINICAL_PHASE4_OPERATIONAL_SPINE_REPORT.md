# Clinical Services Phase 4 Operational Spine Report

Generated: 2026-06-09

## Scope

Implemented the core operational spine for TOTTECH ONE HMS:

- Patient Timeline / Patient 360 timeline route
- Revenue and billing engine foundation
- Insurance / TPA schema foundation
- PDF document payload engine
- Document repository
- Audit trail viewer
- Tenant / hospital / branch security validation script

No new dashboards, KPI widgets, or analytics pages were added.

## Database

Migration added:

- `prisma/migrations/202606091730_clinical_phase4_operational_spine/migration.sql`

Created / extended:

- `patient_timeline_events`
- `billing_invoices`
- `billing_invoice_items`
- `payments`
- `refunds`
- `payment_modes`
- `insurance_providers`
- `insurance_policies`
- `insurance_pre_authorizations`
- `insurance_claims`
- `insurance_settlements`
- `document_repository`
- `claim_documents`

Default payment modes inserted:

- Cash
- Card
- UPI
- Bank Transfer
- Insurance
- Corporate Credit

## Patient Timeline

Created:

- `/clinical-services/patients/[id]/timeline`
- `/api/clinical/patients/[id]/timeline`

Capabilities:

- Chronological timeline
- Event type filter
- Search
- Details drawer
- Print
- CSV export
- PDF export registration through document repository

Timeline events are generated by clinical workflow saves through:

- `lib/clinical/phase4-operational-spine.ts`

## Billing Engine

Created:

- `/api/clinical/billing/invoices`
- `/api/clinical/billing/invoices/[id]`
- `/api/clinical/billing/invoices/[id]/pdf`

Capabilities:

- Invoice list
- Invoice details
- Auto billing item creation from clinical workflows
- Payment posting
- Refund request creation
- Invoice recalculation
- Branded invoice PDF payload

Auto billable workflow mapping:

- Consultation -> Consultation Fee
- Lab -> Lab Charges
- Radiology -> Radiology Charges
- Pharmacy -> Pharmacy Charges
- IPD -> Admission Charges
- Bed Management -> Bed Charges
- OT -> OT Charges
- ICU -> ICU Charges
- IVF -> IVF Package Charges

## Document Engine

Created:

- `/api/clinical/documents`
- `/api/clinical/documents/[id]/pdf`

Capabilities:

- Register generated/uploaded documents
- Store version, source module, source record, patient, file metadata
- Generate branded PDF payload with hospital/branch branding context
- Timeline event creation for generated documents

Document repository supports:

- Prescriptions
- Reports
- Consents
- Insurance documents
- Claims documents
- Invoices
- Timeline exports

## Audit Trail

Created:

- `/administration/audit-trail`
- `/api/clinical/administration/audit-trail`

Filters:

- User
- Patient
- Module
- Date range
- Hospital/branch through clinical context

## Security Validation

Created:

- `scripts/clinical-phase4-security-validation.sql`

Validation result:

- All Phase 4 spine tables include `tenant_id`, `hospital_id`, and `branch_id`.

## Integration With Phase 3

Updated:

- `lib/clinical/phase3-domain-storage.ts`

Every normalized clinical workflow save now calls:

- `recordWorkflowSpineEffects`

This creates:

- Patient timeline event
- Billing item where workflow is billable
- Billing timeline event
- Audit evidence

## Validation

Commands run:

- Applied Phase 4 SQL migration with `psql`
- Granted app DB user access to new tables and sequences
- Ran `scripts/clinical-phase4-security-validation.sql`
- `npx tsc --noEmit --pretty false`
- `npm run build`

Results:

- Security validation passed
- TypeScript passed
- Production build passed

## Notes

The PDF engine currently returns structured branded PDF payloads, matching the existing application pattern. A later enhancement can add binary PDF rendering if a PDF renderer dependency is introduced.
